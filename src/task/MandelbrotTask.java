package task;

public class MandelbrotTask extends Task {
    //    que hace el grafico del conjunto de Mandelbrot para una regíon dada.
    @Override
    public void run() {
    // toma los p ́ıxeles de la regi ́on y eval ́ua el color que
    // deben tener.
    }
}
